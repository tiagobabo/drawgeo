<%= form_for(@draw_user) do |f| %>
  <% if @draw_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@draw_user.errors.count, "error") %> prohibited this draw_user from being saved:</h2>

      <ul>
      <% @draw_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_draw %><br />
    <%= f.number_field :id_draw %>
  </div>
  <div class="field">
    <%= f.label :id_user %><br />
    <%= f.number_field :id_user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
